# Core — required
numpy>=1.24

# Optional enhanced backend — robust Touchstone I/O + passivity-tested vector
# fitting that yields a null-accurate AND passive SPICE subcircuit on measured
# .s2p import. Auto-detected (src/s2p_tool/skrf_backend.py); when absent the tool
# falls back to the numpy-only Foster synthesis. Pulls scipy.
scikit-rf>=1.9
matplotlib>=3.7   # only for plot_error.py (error-curve PNG); not core
pypdf>=4.0        # datasheet text extraction (--pdf / Datasheet→S2P)
PyMuPDF>=1.24     # datasheet vector-graph curve extraction (Datasheet→S2P tab)
openpyxl>=3.1     # Komponent Analizi → Excel toplama (aggregate .xlsx)

# GUI dependencies
PyQt5>=5.15

# Build tool for creating .exe
pyinstaller>=6.0